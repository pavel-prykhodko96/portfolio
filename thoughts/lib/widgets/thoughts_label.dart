import 'package:flutter/material.dart';

class ThoughtsLabel extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Padding(
      padding: const EdgeInsets.fromLTRB(8, 8, 8, 8),
      child: Image.asset("assets/thoughtsLabel.png"),
    );
  }
}
